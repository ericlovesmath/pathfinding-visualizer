import { node, grid } from './grid.js';

export function astar(grid: grid) {
    console.log("Hello");
}

function adjNodes(grid: grid, node: node): node[] {
    let nodes: node[] = [];
    // if (node.x >= 0 )

    return nodes;
    
}
